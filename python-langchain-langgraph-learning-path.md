# Python + LangChain/LangGraph 学习路线

本文提取并扩展 `ai-agent-development-guide.md` 的第四部分，面向希望先掌握“工作流 + Agent 节点”，再逐步学习自主 Agent 和多 Agent 的开发者。

## 一、原文第四部分

### 有 Java/Spring Boot 基础程序员的 Python + LangChain/LangGraph 学习路线

后续开发统一使用 Python、LangChain 和 LangGraph，不使用 Java 语言或 Java Agent 框架。Java/Spring Boot 经验仍然有价值：数据库、事务、并发、权限、微服务和监控等工程能力可以直接迁移。

如果你的目标是“开始编写真正的 AI Agent”，我更推荐：

> **LangGraph 为主，LangChain 作为组件库。**

简单区分：

- **LangChain**：适合快速调用模型、Prompt、工具、检索、RAG、结构化输出。
- **LangGraph**：适合编排 Agent 的状态、步骤、循环、分支、人工介入、错误重试和多 Agent 协作。

可以这样理解：

> **LangChain 是零件库，LangGraph 是流程控制器。**

### 第一阶段：补齐 Python 与异步编程基础

重点学习函数、类、异常、类型标注、虚拟环境、包管理、`async/await`、HTTP 请求、JSON、日志和 pytest。能读懂并修改一个小型 Python 项目即可，不必先学习数据科学或模型训练。

### 第二阶段：掌握大模型 API 与 LangChain Core

使用 OpenAI 兼容 API 完成普通对话、流式输出、多轮消息、Prompt 模板、结构化输出和 Token/费用统计。然后学习 LangChain 的 `ChatModel`、消息、Runnable、Parser 和回调机制，理解模型的不确定性与上下文窗口限制。

### 第三阶段：用 LangGraph 构建有状态工作流

掌握 `StateGraph`、节点、边、条件分支、循环、`MessagesState`、checkpoint 和 `thread_id`。先实现“输入 → 模型 → 输出”的单 Agent，再加入重试、最大步数、超时和人工确认。当前仓库的 CLI 对话 Agent 就是这一阶段的最小实践。

### 第四阶段：工具调用与可靠执行

用 Python 函数或 LangChain Tool 接入 REST API、数据库、文件和业务服务，学习参数校验、结构化结果、权限控制、幂等性、错误恢复和审计日志。让模型负责选择工具，让代码负责真正执行；付款、删除、状态变更等高风险操作必须人工确认。

### 第五阶段：RAG 与记忆

完成文档解析、切分、Embedding、向量库检索、引用回答和“无法确认时明确说不知道”。区分短期会话状态、摘要记忆与长期业务记忆，避免把全部历史消息无限发送给模型。

### 第六阶段：Agent 编排、MCP 与评估

学习 Planner-Executor、ReAct、子图和多 Agent 协作，但优先采用“工作流 + Agent 节点”。了解 MCP 工具协议，并建立包含答案质量、工具调用、权限边界、幻觉、延迟和成本的评测集。

### 第七阶段：生产化部署

补齐配置管理、密钥保护、缓存、并发控制、重试/熔断、链路追踪、Prompt 版本、数据脱敏、权限审计、Docker 部署和模型降级。为每个 Agent 设置最大步数、超时和预算，防止死循环与成本失控。

## 二、先确定技术栈

推荐的基础组合如下：

| 层次 | 选择 | 作用 |
| --- | --- | --- |
| 语言 | Python 3.11+ | AI SDK、异步 I/O、数据处理和 Web 生态最完整 |
| 流程编排 | LangGraph | 用状态图显式描述节点、边、分支、循环和暂停 |
| AI 组件 | LangChain / LangChain Core | 消息、Prompt、Tool、Parser、Retriever 和模型适配 |
| OpenAI 兼容模型 | `langchain-openai` | 统一接入 OpenAI、DeepSeek、OpenRouter 等服务 |
| 配置 | `python-dotenv` | 从 `.env` 加载 API Key 和模型配置 |
| 测试 | pytest | 测试节点、工具、路由和异常分支 |
| Web（后续） | FastAPI | 将稳定的图封装成 HTTP API |
| 持久化（后续） | PostgreSQL + checkpoint，或 Redis | 保存会话、任务状态和恢复点 |

不建议把 CrewAI、AutoGen 等高层多 Agent 框架作为起点。它们可以在后期用于特定协作模式，但初学时应先理解状态和执行边界。

## 三、分阶段学习计划

### 阶段 0：建立开发环境（1 天）

目标是能够独立创建项目、安装依赖、运行测试，并且不把密钥提交到 Git。

```bash
python3.11 -m venv .venv
source .venv/bin/activate       # Windows: .venv\\Scripts\\activate
python -m pip install -U pip
python -m pip install langchain langgraph langchain-openai python-dotenv pytest
```

应掌握：虚拟环境、`requirements.txt`/`pyproject.toml`、环境变量、模块导入、Git 忽略 `.env`。先使用一个 OpenAI 兼容接口完成最小对话，不要同时接入多个模型供应商。

### 阶段 1：Python 工程基础（1–2 周）

重点不是学习数据科学，而是能可靠地编写 Agent 的业务代码：

- 函数、类、模块、包和依赖注入；
- `dict`、`list`、`dataclass`、泛型和类型标注；
- 异常分类、`try/except/finally`、自定义异常；
- 文件、JSON、HTTP 请求、序列化和编码；
- `async/await`、超时、并发任务与资源释放；
- `logging`，而不是到处使用 `print`；
- pytest 的 fixture、mock、参数化测试。

练习：写一个读取 JSON 的天气/汇率客户端，加入超时、重试、错误日志和单元测试。完成标准是：没有 API Key 时程序给出明确错误，网络失败时不会无限等待。

### 阶段 2：模型 API 与 LangChain Core（1–2 周）

先直接理解 HTTP 层，再使用框架封装。掌握：

1. `system`、`user`、`assistant`、`tool` 消息的区别；
2. 普通调用、流式输出、多轮消息和上下文窗口；
3. Prompt 模板中的变量、版本化和注入风险；
4. `ChatModel`、Runnable 管道（`prompt | model | parser`）；
5. Pydantic 结构化输出和解析失败处理；
6. Token、延迟、重试和费用记录。

练习：实现一个“文本分类器”，输出固定的 Pydantic 对象（类别、理由、置信度），并为非法 JSON、空响应和模型超时写测试。不要把模型返回的字符串直接当作 SQL、Shell 或业务参数执行。

### 阶段 3：LangGraph 状态工作流（1–2 周）

把 Agent 看成一个有状态的程序，而不是一段神奇的 Prompt。核心概念：

- **State**：节点共享的输入、输出和中间数据，尽量定义窄而明确的字段；
- **Node**：普通 Python 函数或模型调用，负责一个可测试的步骤；
- **Edge**：固定转移；
- **Conditional edge**：根据状态路由到不同节点；
- **Loop**：带明确退出条件和最大次数的循环；
- **Checkpoint**：保存状态，使任务可恢复、可暂停；
- **thread_id**：隔离不同会话或任务实例。

建议按以下顺序扩展：

```text
输入 → 模型 → 输出
输入 → 分类 → 检索/直接回答
输入 → 模型 → 工具 → 模型 → 输出
                         └→ 失败重试/人工确认
```

当前仓库的最小实现见 [`app/graph.py`](app/graph.py)：`StateGraph` 在 68–72 行定义图，64–66 行是模型节点，`MemorySaver` 保存进程内会话。先将它改造成“分类节点 + 两条回答路径”，再增加一个受控循环。

必须加入的保护：最大步数、单节点超时、模型重试次数、循环退出条件、取消任务和错误状态。图的每个节点都应能脱离模型单独测试。

### 阶段 4：工具调用与可靠执行（2–3 周）

工具是 Agent 连接现实系统的边界。学习顺序：

1. 将纯函数包装成 Tool，并定义清晰的参数 schema；
2. 为参数做类型校验、范围校验和业务权限校验；
3. 将“查询类工具”和“有副作用工具”分开；
4. 为写操作设计幂等键、重复请求检测和事务边界；
5. 统一工具错误格式，让模型知道是可重试还是必须停止；
6. 记录调用者、参数摘要、结果摘要、耗时和审计 ID。

安全原则是：模型可以提出动作，代码决定是否执行。付款、删除、发邮件、修改库存或权限等操作必须经过显式授权和人工确认。工具返回的网页或文件内容也应视为不可信输入，防止 Prompt Injection。

练习项目：做一个“订单查询 Agent”。查询订单可以自动执行；取消订单先生成待确认计划，用户确认后才调用写操作工具。测试重复提交、越权订单号、超时和下游 500 错误。

### 阶段 5：RAG 与记忆（2–3 周）

RAG 学习链路应完整覆盖：文档加载 → 清洗 → 按语义切分 → Embedding → 向量存储 → 相似检索 → 重排/过滤 → 带来源回答。

重点细节：

- 切块要保留标题、页码、权限和来源元数据；
- 检索结果必须做权限过滤，不能只按相似度返回；
- Prompt 要求引用来源，证据不足时明确说不知道；
- 评估检索召回率和答案正确率，不要只看最终文本；
- 区分短期消息状态、摘要记忆和长期业务事实，不能无限拼接历史消息。

先用本地小文档和 Qdrant/pgvector 做可重复实验，再考虑复杂的混合检索和重排模型。

### 阶段 6：高级编排、MCP 与评估（持续学习）

在单 Agent 工作流稳定后，再学习：

- Planner-Executor：规划与执行分离，并限制计划长度；
- ReAct：理解“思考/行动/观察”循环及其风险；
- 子图：把复杂流程封装成可复用模块；
- 多 Agent：只在角色边界清晰、收益可测量时引入；
- MCP：统一发现和调用外部工具，仍需在本地做权限控制。

建立固定评测集，至少覆盖：答案质量、工具选择正确率、参数正确率、任务完成率、越权率、幻觉率、平均延迟、Token/费用、人工介入比例和重复执行率。每次修改 Prompt、模型或路由都重新跑评测。

### 阶段 7：生产化（按项目需要）

将 CLI 迁移为服务前，补齐：

- 密钥管理和配置分层（开发/测试/生产）；
- 请求限流、并发控制、缓存、重试和熔断；
- 日志脱敏、链路追踪、指标和告警；
- Prompt、模型和工具 schema 的版本管理；
- checkpoint 的数据库持久化与数据清理策略；
- Docker 部署、健康检查和优雅停止；
- 模型降级、预算上限、最大运行时间和人工接管。

## 四、一个可执行的毕业项目

建议实现“企业知识与工单助手”：

1. 接收用户问题并识别意图；
2. 对知识库问题执行带权限的 RAG；
3. 对工单问题调用查询工具；
4. 对创建/关闭工单生成计划并请求确认；
5. 保存会话 checkpoint 和审计日志；
6. 用 30–50 条固定案例评估答案、工具和权限。

完成这个项目后，再把某一个节点替换为 Agent，自主程度逐步增加，而不是一次性把整个系统交给模型。

## 五、完成标准与常见误区

达到下一阶段前，应该能回答：状态由谁写入？失败从哪里恢复？工具是否可重复执行？用户是否有权限？模型说错时系统会怎样？

常见误区包括：一开始追逐多 Agent；把模型输出当作可信代码或 SQL；没有超时和最大步数；把所有历史消息永久发送；只凭主观体验评估；把 API Key 写进代码；用框架隐藏了 HTTP、消息和错误处理细节。

推荐的最终路线是：

```text
Python 工程基础
  → 模型 API / LangChain Core
  → LangGraph 状态图
  → Tool Calling 与可靠执行
  → RAG 与记忆
  → MCP / 子图 / 受控多 Agent
  → 评估与生产部署
```

